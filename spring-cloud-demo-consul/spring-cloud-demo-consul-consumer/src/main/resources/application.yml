spring:
  application:
    name: spring-cloud-consul-consumer
  cloud:
    consul:
      port: 8500
      host: localhost
      discovery:
        register: false  #设置不需要注册到consul
server:
  port: 8124

#feign 配置
#feign:
#  hystrix:
#    enabled: true
#  client:
#    config:
#      default:
#        connectTimeout: 5000
#        readTimeout: 5000
#        loggerLevel: full