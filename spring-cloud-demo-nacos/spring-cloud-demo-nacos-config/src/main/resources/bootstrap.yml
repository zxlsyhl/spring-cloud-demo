server:
  port: 8129
spring:
  profiles:
    active: dev
  application:
    name: nacosconfig
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: nacosconfig
