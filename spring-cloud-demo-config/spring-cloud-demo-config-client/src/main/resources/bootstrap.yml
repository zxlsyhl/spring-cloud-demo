spring:
  application:
    name: config-client                     #指定了配置文件的应用名
  cloud:
    config:
      uri: http://localhost:7001/        #Config server的uri
      profile: dev                       #指定的环境，profile
      label: master                      #指定分支
server:
  port: 8000